# BAD: 0.0.0.0 port binding
# Expected: deny — "binds to 0.0.0.0"
services:
  db:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "0.0.0.0:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 3
